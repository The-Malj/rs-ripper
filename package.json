{
  "name": "ripperv2-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "serve:alt1": "vite preview --host 127.0.0.1 --port 4173",
    "release:check": "npm run typecheck && npm run validate:fixtures && npm run validate:perf && npm run build",
    "typecheck": "tsc --noEmit",
    "validate:reference-ui": "tsx tests/validate_reference_ui.ts",
    "validate:fixtures": "tsx tests/fixtures_bootstrap.ts",
    "validate:perf": "tsx tests/perf_budget.ts"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "alt1": "^0.1.2",
    "tesseract.js": "^7.0.0",
    "vite": "^7.3.1"
  }
}
